# سجل القرارات

| الرقم | التاريخ | القرار | السبب | الحالة |
|---|---|---|---|---|
| D-001 | 2026-08-02 | اعتماد `APPROVED_REQUIREMENTS.docx` وحده مرجعًا حاكمًا، ببصمة `6cb2e99449deb287b2008baf23e091efe45a89f3edfb15df721c933271d6b65b`. | النسخ السابقة لا تتضمن جميع التعديلات. | معتمد |
| D-002 | 2026-08-02 | التطبيق والخدمات يجب أن تعمل دون اشتراك أو تكلفة إلزامية ودون بطاقة بنكية. | شرط صريح من المستخدم. | معتمد |
| D-003 | 2026-08-02 | تقسيم التنفيذ إلى مراحل وفروع وPull Requests مع تحقق قبل الدمج. | منع تضارب الجلسات وادعاء الاكتمال غير المثبت. | معتمد |
| D-004 | 2026-08-02 | GitHub للكود والوثائق فقط، لا لبيانات العملاء الحقيقية. | الخصوصية وتقليل أثر التسرب. | معتمد |
| D-005 | 2026-08-02 | عبارات «مسودة» و«بانتظار الاعتماد» المتبقية داخل ملف Word عبارات شكلية تجاوزها اعتماد المستخدم الصريح، مع إبقاء الملف دون تعديل للحفاظ على بصمته. | منع التباس الجلسات اللاحقة دون تغيير المرجع الأصلي. | معتمد |
| D-006 | 2026-08-03 | اعتماد Cloudflare Workers Free للـAPI، و**Cloudflare Workers Static Assets** لاستضافة الواجهة الأساسية، وCloudflare D1 Free للبيانات، مع Firebase Authentication Spark للمصادقة فقط بطريقة **Email/Password** لحسابين ينشئهما المشرف. يبدأ التشغيل على نطاق `workers.dev` المجاني ولا يشترط نطاقًا مدفوعًا؛ وCloudflare Pages بديل احتياطي يحتاج قرارًا لاحقًا. يعطل self-sign-up وحذف الحساب من المستخدم وPhone/SMS وAnonymous وأي مزود آخر. كل API خاص يعمل Fail closed، ويتحقق من Firebase ID Token وفق `alg=RS256` و`kid` المطابق لشهادة X.509 العامة والتوقيع و`aud` و`iss` و`exp` و`iat` و`auth_time` و`sub`، ثم قائمة سماح لشخصين داخل D1. ترتبط D1 بالـWorker داخليًا ولا يسمح بتجاوزه. يفرض `app_users` مستخدمين نشطين فقط ودورًا نشطًا فريدًا، ويمنع `INSERT OR REPLACE` في provisioning. تخزن القيم المالية كعدد صحيح من الهللات ضمن المجال الآمن، ويحظر floating point. في بداية S3 يجب قياس تحقق الرمز على Workers Free؛ إذا تكرر تجاوز حد CPU البالغ 10ms يتوقف التنفيذ وتعود المسألة إلى بوابة القرار أو بديل مجاني، ويحظر Workers Paid أو الفوترة. يحظر Zero Trust وBlaze وأي Billing Account أو وسيلة دفع، ولا تنشأ خدمة سحابية دون موافقة مستقلة. | يحقق شرط صفر تكلفة إلزامية، ويوفر قاعدة علائقية قابلة للتصدير وهوية منفصلة، مع فصل مزود الهوية عن البيانات وتقليل مخاطر المصادقة المخصصة وقفل البيانات. تفاصيل التنفيذ والقيود في `docs/architecture/ADR-001-FREE-ARCHITECTURE.md` و`docs/architecture/FINANCIAL_INTEGER_RULE.md`. | معتمد |

## الأسئلة المحسومة والقرارات المعتمدة إشرافيًا لـ S6 و S7

| الرقم | السؤال الأصلي | قرار الحسم المرتبط | الحالة |
|---|---|---|---|
| Q-001 | هل تغيير النسبة الاستثنائية لعمل معين يحتاج موافقة الطرفين، أم يكفي تسجيل السبب وسجل التدقيق؟ | D-009 | محسوم |
| Q-002 | ما آلية تصحيح أو إلغاء دفعة سبق تسجيلها، وهل تحتاج موافقة الطرفين؟ | D-010 | محسوم |
| Q-003 | هل سيُعتمد إقفال شهري للتسويات وآلية لإعادة فتحها؟ هذه الميزة ليست معتمدة في المرجع الحالي ولا يجوز افتراضها. | D-011 | محسوم |
| Q-004 | ما قاعدة التقريب الحاكمة عند حساب نسب مالية تنتج كسور هللة؟ لا يجوز افتراض التقريب قبل اعتماد قاعدة صريحة. | D-012 | محسوم |

## D-009 إلى D-012 — القرارات الإشرافية المعتمدة لإغلاق S4 وتمهيد S6 و S7

| الرقم | التاريخ | القرار | السبب | الحالة |
|---|---|---|---|---|
| D-009 | 2026-08-12 | تغيير النسبة الاستثنائية لعمل معين يتطلب موافقة الحسابين المختلفين، مع حفظ السبب والطالب والموافق والتوقيت وسجل التدقيق. لا يستطيع مقدم الطلب اعتماد طلبه بوصفه الموافقة الثانية. | حسم Q-001 لمتطلبات S6 وضمان فصل المسؤوليات. | معتمد |
| D-010 | 2026-08-12 | تصحيح أو إلغاء دفعة مسجلة يتم بقيد عكسي موثق، دون حذف أو تعديل يمحو القيد الأصلي، مع سبب وموافقة الحسابين المختلفين وسجل تدقيق كامل. | حسم Q-002 لمتطلبات S7 وضمان سلامة السجلات المالية وعدم المساس بالسجل التاريخي. | معتمد |
| D-011 | 2026-08-12 | اعتماد إقفال شهري ناعم للتسويات. بعد الإقفال لا تعدل الفترة المقفلة بصورة عادية. إعادة الفتح استثنائية وتتطلب موافقة الحسابين المختلفين مع السبب والطالب والموافق والتوقيت وسجل التدقيق. لا حذف أو إعادة كتابة للتاريخ. | حسم Q-003 لمتطلبات S7 وتوفير انضباط دوري للتسويات. | معتمد |
| D-012 | 2026-08-12 | عند ناتج مالي يحتوي كسور هللة، يكون التقريب إلى أقرب هللة، وعند التعادل الدقيق 0.5 هللة يستخدم half-up إلى الهللة التالية. تبقى القيم المخزنة أعدادًا صحيحة من الهللات وفق D-006. | حسم Q-004 لمتطلبات S7 واتساقًا مع قاعدة الأعداد الصحيحة للهللات (D-006). | معتمد |
| D-013 | 2026-08-12 | اعتماد استثناء تشغيلي خاص بتنفيذ S5 / Issue #4 فقط؛ يفوض المساعد بـ self-review و self-repair وإعادة الاختبارات والـ CI والدمج الذاتي (self-merge) عبر FQ/PRs عبر الفروع مع إلزامية final-head CI و Squash merge، ودون أي تعديل مباشر على main. لا يشمل هذا الاستثناء إعلان S5_COMPLETE أو إغلاق Issue #4 أو بدء S6 أو تحريك stable refs أو إجراء Cloud writes. المراجعة الإشرافية النهائية تظل إلزامية بعد انتهاء S5. | تسريع تنفيذ S5 / Issue #4 وفق التفويض الإشرافي الصريح، مع الحفاظ على صرامة الحوكمة والتحقق. | معتمد |
| D-014 | 2026-08-12 | يدخل Work في التسوية الشهرية بحسب `confirmed_at` authoritative confirmation timestamp nullable فقط؛ Work بلا `confirmed_at` لا يدخل التسوية، ولا يجوز استبداله بـ `created_at` أو تاريخ السعر أو حالة مستنتجة. تغيير `confirmed_at` يحفظ audit/version ويحمي الشهرين القديم والجديد من التعديل بعد الإقفال. | حسم أساس فترة Work للتسوية دون استنتاج زمني غير معتمد. | Approved |
| D-015 | 2026-08-12 | معنى `final_balance_halalas > 0` أن person_1 owes person_2، والسالب بالعكس. يدخل في الرصيد approved client receipts بحسب دور `received_by` مع استبعاد reversals المعتمدة؛ prior balance هو final balance لأحدث تسوية شهرية سابقة valid، والصفر عند عدم وجودها. يمنع `prior_balance_riyals` من أن يكون حقيقة مالية يحددها العميل. الهوية: `prior_balance + person_2_work_share - approved_receipts_received_by_person_2 - net_transfers_to_person_2 + half_subscription_cost - half_transfer_fees + governed_expense_net_effect_to_person_2`. | حسم convention الرصيد النهائي، receipts، carry-forward، وإشارة التسوية مع إبقاء المصروف العام غير المحكوم fail-closed. | Approved |
| D-016 | 2026-08-12 | لا يوجد daily prorating للاشتراك. يصبح aggregate subscription change أو cancellation المسجل أثناء الشهر M نافذًا لتسوية الشهر M+1. يبقى P-01 baseline aggregate بقيمة `13,650` halalas لاشتراكين حتى يصبح change محكومًا نافذًا. لا تخترع أسماء أو مبالغ فردية، وتبقى snapshots المقفلة immutable. | حسم تفعيل الاشتراكات effective-dated على مستوى الشهر التالي دون اختراع prorating أو تفاصيل فردية. | Approved |
| D-017 | 2026-08-13 | لِـ`NO_PRICE` يبدأ clock من `created_at` ما دام Work في `PRICE_UNSET`. لِـ`NO_REPLY` يبدأ من آخر transition إلى `WAITING_CLIENT_RESPONSE`، أو من `created_at` إن كانت الحالة الابتدائية كذلك. لِـ`NO_PAYMENT` يبدأ من `confirmed_at` للعمل المؤكد ذي السعر الموجب، ويستمر فقط ما دامت approved receipts تساوي `0`. المدد الثلاث configurable من `s8_alert_settings` ولا توجد default thresholds؛ alert بلا إعداد ظاهر كـ`NOT_CONFIGURED` ولا يطابق عناصر. | حسم FR-029 وanchors الزمنية دون استنتاج مالي خارج S6/S7 أو اختراع مدد تشغيلية. | APPROVED |
| D-018 | 2026-08-13 | في S9 / Issue #8 يُستبدل شرط الاختبارات اليدوية الموثقة لأحجام الشاشة وRTL ببوابة قبول آلية deterministic وقابلة لإعادة الإنتاج، دون اشتراط تدخل بشري لإغلاق S9. تعتمد البوابة Playwright متعدد المحركات مع responsive/RTL/keyboard/touch/accessibility/error/double-submit/function-parity checks، ولا يجوز اعتبار visual snapshot وحده دليل قبول. لا يغيّر هذا القرار أي قاعدة عمل أو معنى مالي، وأي معنى غير محسوم يبقى fail-closed. | تحقيق توجيه المستخدم بصفر تدخل بشري في QA مع جعل القبول موضوعيًا ومتكررًا وقابلًا للإثبات في CI بدل الفحص اليدوي غير القابل للتكرار. | APPROVED |
| D-019 | 2026-08-13 | استثناء تنفيذي خاص بـS9 فقط يسمح بتسليم متتابع داخل PR التنفيذ نفسه: Codex Sol High هو المنفذ الأول ويكمل التنفيذ والاختبارات وself-review ثم يتوقف نهائيًا عن الفرع؛ بعد ذلك فقط يستلم Manus للمراجعة والإصلاح المباشر وإعادة الاختبارات وfinal-head CI. يمنع أي عمل متزامن بينهما، ولا يملك أي منهما حق الدمج. بعد Manus تجري مراجعة إشرافية نهائية واحدة ثم Squash merge إذا كانت النتيجة نظيفة. لا يمتد هذا الاستثناء تلقائيًا إلى S10 أو S11. | تقليل الوقت واستهلاك رصيد Codex ومنع حلقات الإرجاع بين المنفذين مع إبقاء تسلسل الملكية واضحًا والمراجعة الإشرافية النهائية مستقلة. | APPROVED |
| D-020 | 2026-08-13 | في S10، تعني «إعادة اختبار AC-01..AC-14» إعداد تقرير حالة واختبار لما هو منفذ، ولا تسمح بسحب تنفيذ مرحلة أخرى إلى S10. يبقى `AC-11` تابعًا لـS3 المؤجلة، ويبقى `AC-14` تابعًا لـS11. يتحقق S10 فقط من عدم وجود مسار حالي يتجاوز هذه الحدود، ولا يُعتبر تأجيلهما وحده فشلًا في S10، ولا يجوز تسجيل PASS زائف لهما. | حسم تعارض نطاق Issue #9 مع مصفوفة التتبع دون سحب تنفيذ S3 أو S11 إلى S10، مع إبقاء تقرير القبول صادقًا وقابلًا للتدقيق. | APPROVED |
| D-021 | 2026-08-13 | اعتماد حوكمة تشغيل Live/Deployment قابلة للاستئناف ومضادة للحلقات لأي تشغيل سحابي لاحق، بما في ذلك إعادة فتح S3 أو التشغيل الفعلي بعد S11. كل بوابة ناجحة تصبح checkpoint مرتبطة بـexact source SHA وruntime/payload hash وهوية الحساب والمشروع والخدمة والمدخلات المؤثرة، ولا تعاد إلا إذا تغير مدخل مؤثر، أو ظهر دليل Provider يناقضها، أو تلف دليلها، أو أعاد الإشراف فتحها صراحة. يمنع restart-from-step-1؛ الاستئناف يكون من أول بوابة invalidated فقط. فحص Free/No-Billing/No-Card/No-PayGo ينفذ مرة واحدة لكل هوية حساب وخدمة وإعداد معتمد ولا يعاد دوريًا إلا عند تغيير فعلي أو دليل مباشر معاكس، ويحظر أي تفعيل Billing أو Blaze أو Workers Paid أو PayGo أو بطاقة. المصادقة التفاعلية تنفذ مرة واحدة وتحفظ جلسات المشغل الموجودة؛ يجوز استخدام Chrome أو المتصفح الخارجي عندما يلزم Google login، ولا تعاد المصادقة إلا بدليل صريح على session/token expired أو revoked أو invalid أو تغير الصلاحيات، ولا تعد stderr أو progress messages وحدها فشلًا. قبل أول Cloud write تنفذ Pre-Live Certification واحدة تشمل toolchain وPATH وPython وOpenSSL والـCLI، exact main/source/runtime parity، auth، free-tier/billing boundary، provider permissions، وحالة recovery/cleanup، ثم يجمد المصدر والruntime لهذه الدورة. retries مسموحة فقط للأخطاء المصنفة transient؛ إذا تكرر failure fingerprint نفسه ثلاث مرات دون تغير provider state يعمل circuit breaker ويصدر blocker تشخيصيًا بدل التكرار. لا تنفذ إصلاحات مصدر تلقائية داخل Live loop؛ إذا ظهر defect برمجي يوقف Live ويعالج عبر فرع وPR واحد واختبارات، ثم تستأنف فقط البوابة المتأثرة وتبقى checkpoints غير المتأثرة صالحة. حفظ state/checkpoints ذري ومتين، والمراقبة read-only ولا يجوز أن تسقط writer. recovery يستعيد فقط الموارد المملوكة للتشغيل بهوية مثبتة؛ cleanup idempotent ولا يحذف موردًا أو جلسة سابقة غير مملوكة، ويحافظ على الجلسات القائمة ما لم يوجد compromise أو انتهاء صريح، وتمسح فقط أسرار الـRun المؤقتة. عند الحاجة لتدخل بشري يعرض إجراء واحدًا محددًا، وبعده يستأنف نفس البوابة دون إعادة ما سبق. لا يغير هذا القرار قواعد المنتج أو المعاني المالية ولا يفتح أي صلاحية Cloud write من تلقاء نفسه. | إغلاق الأسباب الموثقة لدوامة S3: إعادة الخطوات الناجحة، تكرار Billing/login، stale runtime/branch، تكرار readiness المتطابق، ضعف تمييز الأخطاء، تعطل state بالمراقبة، وإعادة بناء البيئة بدل الاستئناف؛ مع خفض الوقت والجهد والرصيد دون تخفيف fail-closed أو الخصوصية أو شرط المجانية. | APPROVED |

## D-022 — S3 administrative closure boundary

| الرقم | التاريخ | القرار | السبب | الحالة |
|---|---|---|---|---|
| D-022 | 2026-09-02 | يلغي فقط اعتبار CPU/Telemetry Live gate شرطًا مانعًا لإغلاق S3. لا يغير معمارية D-006 ولا متطلبات الأمن أو المصادقة، ولا يسجل CPU أو Telemetry كـPASS؛ حالتهما `DEFERRED_NON_BLOCKING_BY-D-022`. لا يجيز Live run جديدًا، ولا يسمح Workers Paid أو Billing أو PayGo أو بطاقة أو خدمة مدفوعة. تبقى Firebase authentication وD1 allowlist وfail-closed وexactly-two وAudit نافذة، ولا يخففها القرار. تبقى القرارات والمحاولات السابقة سجلًا تاريخيًا ولا تمحى. الموارد الحالية `RETAINED_OPERATIONAL`. PR #114 غير مطلوب لإغلاق S3 ويظل غير مدموج حتى الإغلاق النهائي. | إغلاق S3 إداريًا بعد إثبات FR-026 وAC-11 عبر PR #115 ونجاح final-head CI، مع إبقاء حدود الأمن والمجانية والسجل التاريخي كما هي. | معتمد إشرافيًا |

## إضافة قرار جديد

يضاف القرار برقم متسلسل، وتاريخ، ونص دقيق، وسبب، وحالة. لا تُعدل القرارات القديمة لمسح التاريخ؛ يُضاف قرار لاحق يلغي أو يستبدل السابق. لا يتحول أي بند `Q` إلى قاعدة تنفيذية إلا بعد تسجيل قرار `D` معتمد.

## D-023 إلى D-027 — UAT Remediation بعد S11

| الرقم | التاريخ | القرار | السبب | الحالة |
|---|---|---|---|---|
| D-023 | 2026-09-03 | تبقى `person_1` و`person_2` أدوارًا داخلية فقط. يعرض المنتج الحسابين باسمَي خالد ووليد، وتكون النسبة الافتراضية خالد 70% ووليد 30%. لا يعاد احتساب التاريخ المالي السابق بسبب هذا الافتراض؛ الاستثناءات لكل عمل تحتاج الموافقة الثنائية المعتمدة. | إزالة كشف البنية الداخلية وتثبيت النسبة الافتراضية الجديدة في طبقة المنتج دون تغيير التاريخ. | معتمد |
| D-024 | 2026-09-03 | عند اعتماد إلغاء العمل يبقى السعر والدفعات التاريخية للقراءة، يصبح رصيد العميل صفرًا ولا يرد له مبلغ، وتتوقف العمليات العادية. أساس الحصة الداخلية هو صافي الدفعات المعتمدة بعد القيود العكسية وبآخر نسبة معتمدة قبل الإلغاء. لا يعاد فتح snapshot مقفلة؛ يعالج الفرق في أول فترة مفتوحة بقيد adjustment إضافي append-only. | توحيد الحقيقة المالية للعمل الملغى عبر كل القراءات والتقارير ومنع إعادة كتابة التاريخ. | معتمد |
| D-025 | 2026-09-03 | خالد هو الحساب الإداري/الإشرافي. يسمح له الخادم بإدارة بريدي الحسابين وإرسال Password Reset دون كشف أو تعيين كلمات المرور. لا حذف أو حساب ثالث أو تغيير دور. تغيير البريد يحافظ على UID/الدور ويبطل الجلسات/Refresh Tokens ويسجل Audit. | تثبيت سطح إدارة محدود وآمن للحسابين. | معتمد |
| D-026 | 2026-09-03 | Reset متاح للاختبار فقط عند علامة بيئية UAT صريحة ومتحققة؛ أي غموض أو قيمة افتراضية أو Production يمنع العملية. لا تنفذ Cloud reset في هذه الجولة. | حماية Production وتهيئة مسار UAT قابلًا للتصفير بصورة fail-closed. | معتمد |
| D-027 | 2026-09-06 | حسم UAT-044: عند إلغاء العمل لا يُفرض أن يكون المبلغ النهائي المستحق على العميل صفرًا في جميع الحالات؛ النتيجة المعتمدة قد تكون صفرًا أو جزئية أو كاملة وفق حالة UAT-044 المحسومة، مع بقاء السعر والدفعات والسجل التاريخي محفوظًا. يستبدل هذا القرار فقط الجزء المتعارض من D-024 الذي كان يفرض أن «يصبح رصيد العميل صفرًا» على نحو مطلق. تبقى بقية D-024 غير المتعارضة نافذة، بما في ذلك حفظ التاريخ، توقف العمليات العادية بعد الإلغاء، عدم إعادة فتح snapshot مقفلة، ومعالجة الفروقات بقيد adjustment إضافي append-only في أول فترة مفتوحة. | تسجيل قرار UAT-044 الذي سبق أن حسمه المستخدم دون إعادة فتحه، وإزالة التعارض الحاكم قبل أي تنفيذ Product لاحق. | معتمد |

## D-007 — Stable checkpoint and recovery governance

| Number | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-007 | 2026-08-09 | Models must not work directly on `main`; work starts from a stable checkpoint or from `main` only after explicit verification. A PR is accepted only when CI passes on its final head. Force-push to stable refs and deletion of stable branches/tags are prohibited. A commit is not stable merely because it was merged. Stable promotion requires final-head CI, human supervisory review, SHA registration in `PROJECT_STATE.md`, and an explicit stable checkpoint. If a suspect change series exists, do not repair `main` with a force reset; create a recovery branch from the last stable checkpoint and reintroduce changes through a PR. | Protect the recoverable baseline and separate merging from declaring stability. | Approved |

| D-008 | 2026-08-11 | S3 administrative closure and supervised transition to S4: تغلق دورة S3 الحالية إداريًا بالحالة `CLOSED-BLOCKED/DEFERRED`، ولا تعتبر `S3 COMPLETE` لأن Live acceptance توقفت عند Firebase readiness ولم تصل إلى D1 / Worker / Audit أو CPU / Telemetry. يعتمد هذا القرار الانتقال الإداري الاستثنائي إلى S4 دون تحويل أي `NOT_REACHED` إلى `PASS`. لا يسمح بأي Live Run إضافي أو إصلاح S3 إضافي تلقائي ضمن هذه الدورة؛ وإعادة فتح S3 Cloud acceptance تحتاج قرارًا إشرافيًا مستقلًا. يبقى D-006 وD-007 نافذين، ولا يسمح هذا القرار بخدمة مدفوعة أو Billing أو بطاقة. S4 مصرح بها إداريًا فقط، وحالتها `AUTHORIZED_NOT_STARTED`، ولا يبدأ تنفيذها أو Gate 0 أو Gate 1 إلا بتفويض منفصل. | النتيجة النهائية الموثقة للمحاولة `s3cpu-20260811-205953-6d729287`: `FIREBASE_ADD_READINESS_TIMEOUT` مع HTTP `403` ثابت عبر `25` محاولة، وCleanup PASS وفق الأدلة المرفقة. | معتمد إشرافيًا بموجب التفويض النهائي؛ لا يعلن اكتمال S3 ولا يغير المتطلبات أو القرارات السابقة. |
