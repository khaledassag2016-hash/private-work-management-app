# S2 local prototype

This directory contains a deliberately limited, dependency-free proof for the proposed architecture. It uses only fabricated identifiers and records. Run from repository root:

```bash
python scripts/validate_s2.py
```

It does not connect to Cloudflare or Firebase, create accounts, deploy services, or implement application screens.
